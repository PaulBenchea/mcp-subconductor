export enum AlertType {
  Info = 'info',
  Warn = 'warn',
  Error = 'error'
}

export enum TaskColumn {
  Status = 'Status',
  ID = 'ID',
  Name = 'Name',
  Notes = 'Notes'
}

export enum ChecklistStatus {
  Active = 'Active',
  Idle = 'Idle',
  Done = 'Done',
  Archived = 'Archived'
}

export enum TaskStatus {
  Idle = 'Idle',
  Done = 'Done'
}